# TODO

1. ~~**Redesign** `site/Aurora-OpenAIRE-Usage-Dashboard.html` to use the style, fonts, and colors of [aurora-universities.eu](https://aurora-universities.eu/), using the `/taste-skill` skill (`.claude/skills/taste-skill/`).~~ **Done.** Palette (`#01c8b1` teal, `#008dff` blue, `#0d0a46` ink, `#de53ae`/`#7141f1`/`#ffa255` accents) and fonts (Archivo Black headings, Manrope body) pulled from the site's own CSS, supplied as a saved copy of the page since this sandbox can't fetch that domain directly.
2. ~~**Reorganize the repo** into a folder for the source data and a folder for the HTML/site files, instead of everything living at the repo root.~~ **Done.** CSVs in `data/`, dashboard in `site/`.
3. ~~**Load the CSV data dynamically** in the HTML instead of hand-transcribing it into the `RAW` JS literal.~~ **Done.** `site/Aurora-OpenAIRE-Usage-Dashboard-dynamic.html` loads the 10 CSVs live in-browser via DuckDB-Wasm (SQL over the CSVs), verified against the live GitHub Pages deployment; it's now the default at `index.html`. The static, pre-baked version is kept alongside it as a snapshot/fallback — toggle between them via the "Version" link in either page's header, or `index.html#static` / `index.html#dynamic`.
4. ~~**Add a cost-per-visit benchmark section** at the bottom of the dashboard, to help justify the annual bundle cost to the review committee.~~ **Done.** Computes €/visit from the existing summary stats and weighs it against three adjacent (not directly comparable) industry reference points, with sources.
